Browsers do not sent "+" symbol in URL by default on client side
Przegl¹darki nie przesy³aj¹ symbolu "+" poprzez url

Double-type values in url should use "," instead of "."
Double nale¿y deklarowaæ z przecinkiem zamiast kropki

The "+" symbol is considered as a blank space in url
Symbol "+" jest traktowany jako spacja

To insert it into formula, replace "+" with "%2b" code
Aby przes³aæ plus, zamieñ widoczne znaki "+" na kod "%2b"

Double a String s¹ niespójne w C#

Double 2.0 -> String "2,0"
String "2,0" -> Double 2.0

Przy czym

Double 2,0 -> declaration error
String "2.0" -> DoubleParse error